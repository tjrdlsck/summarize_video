# 🚀 대시보드 개선 및 Whisper 커스텀 설정 도입 보고서

## 💡 주요 변경 사항 및 문제 해결 과정

### 1. 🚫 파일명 검증(Whitelist) 정책 이완 (400 Bad Request 해결)
- **문제 정의**: 사용자가 업로드한 파일명에 한글 중간점(`·`)이나 말줄임표(`…`) 등 특수 기호가 포함된 경우, `SecurityManager`의 `validate_filename()`이 이를 악성(경로 조작 등)으로 간주하여 `400 Bad Request` 에러를 반환했습니다. 이로 인해 "AI 재생성" 시 파일명 검증에 걸려 아무 작업도 실행되지 않았습니다.
- **해결 로직**: 
  - 기존 정규표현식(`r'^[a-zA-Z0-9ㄱ-ㅎ가-힣._\s,\[\]()!&\'"-]+$'`)은 매우 보수적이었습니다.
  - 이를 `r'^[a-zA-Z0-9ㄱ-ㅎ가-힣._\s,\[\]()!&\'"\-·…~+=%]+$'`으로 확장하여 `·`, `…`, `~`, `+`, `=`, `%` 등의 일반적인 기호를 허용하도록 예외 처리했습니다. (경로 조작 문자 `/`, `\` 방어 로직은 그대로 유지)

### 2. 👻 작업 대기열(Task Queue)에서 실패/완료 내역 증발 현상 해결
- **문제 정의**: "AI 챕터 분석하기"를 눌렀으나 이전에 자막 생성이 한 번도 되지 않은 상태여서 예외(FileNotFoundError)가 발생했습니다. 그러나 프론트엔드의 `TaskMonitor`는 `failed`나 `completed` 상태의 작업을 무시하는 백엔드 API(`GET /api/tasks`) 구조 때문에, 작업이 실패하자마자 화면에서 흔적 없이 증발해버렸습니다. 사용자는 진행률을 알 수 없고 "사라진 건지 되고 있는 건지" 혼란에 빠졌습니다.
- **해결 로직**:
  - `services/task_manager.py`의 `get_active_tasks()` 메서드를 수정하여 `failed`, `canceled`, `completed` 상태의 작업 내역도 프론트엔드에 함께 내려보내도록 변경했습니다.
  - `static/js/components.js`의 `TaskMonitor`에서 해당 상태(`failed`, `completed`)를 가진 작업들을 렌더링하고, 무한정 떠있지 않도록 `중단` 버튼을 `닫기` 버튼으로 치환하여 사용자가 직접 확인하고(Dismiss) 삭제할 수 있도록 UX를 개편했습니다.
  - `app/api/routers/tasks.py`의 취소 라우터(`DELETE /api/tasks/{task_id}`) 로직을 수정해, 이미 완료되거나 실패한 작업을 닫을 경우 데이터베이스에서 완전히 삭제(`delete_task()`)하도록 개선했습니다.

### 3. 🎛️ Whisper 사용자 정의(Advanced Settings) 옵션 구현
- **문제 정의**: 하드코딩 되어 있던 Whisper 모델 파라미터를 사용자가 직접 제어하여 다국어 인식이나 특수 도메인 성능을 향상할 수 있게 열어주어야 했습니다.
- **해결 로직**:
  - `VideoUploadModal` 내에 **⚙️ 사용자 정의** 토글을 추가하여 숨김 처리된 패널을 구성했습니다. (최소한의 UI 간섭)
  - `navigator.hardwareConcurrency`와 `navigator.deviceMemory`를 분석하여 사용자의 기기 리소스를 계산하고, 가용 RAM 16GB, 8코어 이상일 경우 `batch_size`를 4로, 8GB 이상일 경우 2로 동적 추천하는 자동화 로직을 도입했습니다.
  - 백엔드의 `services/transcriber.py`에서 파이썬 내장 라이브러리인 `inspect.signature` 모듈을 활용했습니다. 이를 통해 사용자가 설치한 `faster-whisper` 버전에 `batch_size` 지원 기능이 내장되어 있는지 먼저 안전하게 검사한 뒤 동적으로 매개변수를 바인딩하여(`transcribe_kwargs`), 구버전 패키지에서 날 수 있는 하위 호환성 에러를 완벽히 차단했습니다.

## 🧪 권장되는 테스트 및 검증 단계
- 브라우저를 열고 영상을 업로드할 때 **[⚙️ 사용자 정의 (Advanced Settings)]** 토글을 클릭해 보세요.
- 자원 감지를 통해 배치 사이즈(Batch Size)가 자동으로 추천값(예: 2, 4)으로 세팅되어 있는지 확인합니다.
- 원하는 언어, 초기 프롬프트를 임의로 변경하여 작업을 전송하고, 백엔드 콘솔에 `[Whisper Worker] Using batch_size=4` 등의 로그가 출력되는지 점검해 주세요.
